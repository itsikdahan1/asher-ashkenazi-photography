# Optional: public Google Apps Script endpoint used by the admin panel to load/save site content.
# The admin token is not stored here; enter it inside /admin on the Google tab.
VITE_CONTENT_API_URL=""
